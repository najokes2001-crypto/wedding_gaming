<!DOCTYPE html>
<html lang="ru">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Что общего? - Игра на поиск связей</title>
    <style>
        * {
            box-sizing: border-box;
            margin: 0;
            padding: 0;
        }
        
        body {
            background: linear-gradient(135deg, #6a11cb 0%, #2575fc 100%);
            min-height: 100vh;
            font-family: 'Arial', sans-serif;
            padding: 20px;
            font-size: 18px;
            overflow-x: hidden;
            color: #333;
        }
        
        .container {
            max-width: 1200px;
            margin: 0 auto;
            display: flex;
            flex-direction: column;
            align-items: center;
            gap: 30px;
        }
        
        .main {
            background: white;
            padding: 30px;
            border-radius: 20px;
            box-shadow: 0 10px 30px rgba(0,0,0,0.2);
            text-align: center;
            width: 100%;
            position: relative;
            overflow: hidden;
        }
        
        h1 {
            color: #e91e63;
            margin-bottom: 20px;
            font-size: 42px;
            text-shadow: 2px 2px 4px rgba(0,0,0,0.1);
        }
        
        h2 {
            color: #555;
            margin: 20px 0;
            font-size: 28px;
        }
        
        .instructions {
            background: #ffecb3;
            padding: 15px;
            border-radius: 10px;
            margin: 20px 0;
            text-align: left;
            font-size: 18px;
        }
        
        .instructions ul {
            margin-left: 20px;
        }
        
        .game-container {
            display: flex;
            flex-direction: column;
            align-items: center;
            gap: 30px;
            width: 100%;
        }
        
        .photos-container {
            display: flex;
            justify-content: center;
            flex-wrap: wrap;
            gap: 20px;
            margin: 20px 0;
        }
        
        .photo-item {
            width: 300px;
            height: 300px;
            border-radius: 15px;
            overflow: hidden;
            box-shadow: 0 5px 15px rgba(0,0,0,0.2);
            position: relative;
            background: #f5f5f5;
            display: flex;
            align-items: center;
            justify-content: center;
            font-size: 24px;
            font-weight: bold;
            color: white;
            text-shadow: 2px 2px 4px rgba(0,0,0,0.5);
        }
        
        .photo-number {
            position: absolute;
            top: 10px;
            left: 10px;
            background: rgba(0,0,0,0.7);
            color: white;
            width: 30px;
            height: 30px;
            border-radius: 50%;
            display: flex;
            align-items: center;
            justify-content: center;
            font-weight: bold;
            z-index: 2;
        }
        
        .controls {
            display: flex;
            gap: 20px;
            margin-top: 20px;
        }
        
        .my_button {
            background: linear-gradient(45deg, #e91e63, #ff4081);
            color: white;
            border: none;
            padding: 15px 30px;
            border-radius: 20px;
            font-size: 22px;
            cursor: pointer;
            transition: all 0.3s;
            font-weight: bold;
            box-shadow: 0 4px 8px rgba(0,0,0,0.2);
            display: inline-block;
        }
        
        .my_button:hover {
            transform: translateY(-5px);
            box-shadow: 0 8px 15px rgba(233,30,99,0.4);
        }
        
        .my_button:disabled {
            opacity: 0.7;
            cursor: not-allowed;
            transform: none;
        }
        
        .secondary_button {
            background: linear-gradient(45deg, #2196f3, #21cbf3);
            color: white;
            border: none;
            padding: 10px 20px;
            border-radius: 15px;
            font-size: 18px;
            cursor: pointer;
            transition: all 0.3s;
            font-weight: bold;
            box-shadow: 0 4px 8px rgba(0,0,0,0.2);
            display: inline-block;
        }
        
        .secondary_button:hover {
            transform: translateY(-3px);
            box-shadow: 0 6px 12px rgba(33,150,243,0.4);
        }
        
        .answer-container {
            display: none;
            margin-top: 30px;
            animation: fadeIn 0.5s ease;
            background: #e8f5e9;
            padding: 20px;
            border-radius: 15px;
            text-align: center;
            width: 100%;
        }
        
        @keyframes fadeIn {
            from { opacity: 0; }
            to { opacity: 1; }
        }
        
        .answer-text {
            font-size: 24px;
            font-weight: bold;
            color: #e91e63;
            margin: 15px 0;
        }
        
        .progress {
            display: flex;
            justify-content: center;
            margin: 20px 0;
            gap: 10px;
        }
        
        .progress-dot {
            width: 15px;
            height: 15px;
            border-radius: 50%;
            background: #ddd;
            transition: all 0.3s;
        }
        
        .progress-dot.active {
            background: #e91e63;
            transform: scale(1.2);
        }
        
        .progress-dot.completed {
            background: #4caf50;
        }
        
        .score-container {
            position: fixed;
            top: 20px;
            right: 20px;
            background: white;
            padding: 10px 20px;
            border-radius: 20px;
            box-shadow: 0 4px 8px rgba(0,0,0,0.2);
            font-size: 18px;
            font-weight: bold;
            z-index: 100;
        }
        
        .completion-screen {
            text-align: center;
            padding: 30px;
        }
        
        .completion-screen h2 {
            color: #4caf50;
            margin-bottom: 20px;
        }
        
        @media (max-width: 768px) {
            h1 {
                font-size: 32px;
            }
            
            h2 {
                font-size: 24px;
            }
            
            .photo-item {
                width: 250px;
                height: 250px;
            }
            
            .my_button {
                font-size: 20px;
                padding: 12px 25px;
            }
            
            .controls {
                flex-direction: column;
                gap: 10px;
            }
        }
        
        @media (max-width: 480px) {
            .main {
                padding: 20px;
            }
            
            .photo-item {
                width: 100%;
                max-width: 300px;
                height: 250px;
            }
            
            .score-container {
                position: static;
                margin-bottom: 20px;
            }
        }
    </style>
</head>
<body>
    <div class="container">
        <div class="score-container" id="score-container">
            Вопрос: <span id="current-question">1</span>/10
        </div>
        
        <div class="main">
            <h1>🎯 Что общего? 🎯</h1>
            <h2>Найди связь между фотографиями</h2>
            
            <div class="instructions">
                <h3>Как играть:</h3>
                <ul>
                    <li>Посмотрите на три изображения и подумайте, что их объединяет</li>
                    <li>Найдите общее выражение или словосочетание</li>
                    <li>Нажмите кнопку "Показать ответ", чтобы проверить свою догадку</li>
                    <li>Переходите к следующему вопросу, нажав "Следующий"</li>
                </ul>
            </div>
            
            <div class="game-container" id="game-container">
                <div class="photos-container" id="photos-container">
                    <!-- Изображения будут добавлены через JavaScript -->
                </div>
                
                <div class="controls">
                    <button class="my_button" id="show-answer">Показать ответ</button>
                    <button class="secondary_button" id="next-question" disabled>Следующий вопрос</button>
                </div>
                
                <div class="answer-container" id="answer-container">
                    <h3>Правильный ответ:</h3>
                    <div class="answer-text" id="correct-answer"></div>
                    <p id="answer-explanation"></p>
                </div>
                
                <div class="progress" id="progress">
                    <!-- Прогресс будет добавлен через JavaScript -->
                </div>
            </div>
        </div>
    </div>
    
    <script>
        // Данные игры: 10 вопросов с цветными блоками и правильными ответами
        const gameData = [
            {
                colors: ["#FF6B6B", "#4ECDC4", "#45B7D1"],
                texts: ["🔴 Красный", "🟢 Зеленый", "🔵 Синий"],
                answer: "Основные цвета",
                explanation: "Все три являются основными цветами в различных цветовых моделях"
            },
            {
                colors: ["#FF9999", "#FFCC99", "#FFFF99"],
                texts: ["🍎 Яблоко", "🍊 Апельсин", "🍋 Лимон"],
                answer: "Фрукты",
                explanation: "Все три изображения связаны с фруктами"
            },
            {
                colors: ["#C9A0DC", "#A0D2DB", "#A0DCB3"],
                texts: ["🌸 Весна", "☀️ Лето", "🍂 Осень"],
                answer: "Времена года",
                explanation: "Представлены три времени года (кроме зимы)"
            },
            {
                colors: ["#FF5252", "#FF4081", "#E040FB"],
                texts: ["❤️ Сердце", "💖 Любовь", "💜 Страсть"],
                answer: "Чувства и эмоции",
                explanation: "Все связаны с эмоциями и чувствами"
            },
            {
                colors: ["#4CAF50", "#8BC34A", "#CDDC39"],
                texts: ["🌳 Дерево", "🌿 Трава", "🍀 Клевер"],
                answer: "Растительность",
                explanation: "Все элементы относятся к растительному миру"
            },
            {
                colors: ["#2196F3", "#03A9F4", "#00BCD4"],
                texts: ["💧 Вода", "🌊 Море", "💙 Небо"],
                answer: "Водная стихия",
                explanation: "Все ассоциируются с водой и небом"
            },
            {
                colors: ["#795548", "#5D4037", "#4E342E"],
                texts: ["🪵 Дерево", "🍫 Шоколад", "☕ Кофе"],
                answer: "Коричневые объекты",
                explanation: "Все имеют коричневый цвет"
            },
            {
                colors: ["#9E9E9E", "#757575", "#616161"],
                texts: ["☁️ Облако", "🪨 Камень", "⚙️ Механизм"],
                answer: "Серые объекты",
                explanation: "Все имеют серый цвет"
            },
            {
                colors: ["#FF9800", "#FF5722", "#F44336"],
                texts: ["🔥 Огонь", "🌅 Закат", "🍁 Осень"],
                answer: "Теплые цвета",
                explanation: "Все цвета относятся к теплой цветовой гамме"
            },
            {
                colors: ["#3F51B5", "#673AB7", "#9C27B0"],
                texts: ["🌌 Ночь", "🔮 Магия", "👑 Королевство"],
                answer: "Фиолетовые оттенки",
                explanation: "Все цвета - различные оттенки фиолетового"
            }
        ];
        
        let currentQuestionIndex = 0;
        
        // Инициализация игры
        document.addEventListener('DOMContentLoaded', function() {
            // Создаем точки прогресса
            createProgressDots();
            
            // Загружаем первый вопрос
            loadQuestion(currentQuestionIndex);
            
            // Назначаем обработчики событий
            document.getElementById('show-answer').addEventListener('click', showAnswer);
            document.getElementById('next-question').addEventListener('click', nextQuestion);
        });
        
        // Создание точек прогресса
        function createProgressDots() {
            const progressContainer = document.getElementById('progress');
            
            for (let i = 0; i < gameData.length; i++) {
                const dot = document.createElement('div');
                dot.className = 'progress-dot';
                if (i === 0) dot.classList.add('active');
                progressContainer.appendChild(dot);
            }
        }
        
        // Обновление прогресса
        function updateProgress() {
            const dots = document.querySelectorAll('.progress-dot');
            
            dots.forEach((dot, index) => {
                dot.classList.remove('active', 'completed');
                
                if (index < currentQuestionIndex) {
                    dot.classList.add('completed');
                } else if (index === currentQuestionIndex) {
                    dot.classList.add('active');
                }
            });
            
            document.getElementById('current-question').textContent = currentQuestionIndex + 1;
        }
        
        // Загрузка вопроса
        function loadQuestion(index) {
            const question = gameData[index];
            const photosContainer = document.getElementById('photos-container');
            photosContainer.innerHTML = '';
            
            // Создаем элементы для цветных блоков
            for (let i = 0; i < question.colors.length; i++) {
                const photoItem = document.createElement('div');
                photoItem.className = 'photo-item';
                photoItem.style.background = `linear-gradient(135deg, ${question.colors[i]}, ${darkenColor(question.colors[i], 20)})`;
                photoItem.textContent = question.texts[i];
                
                const photoNumber = document.createElement('div');
                photoNumber.className = 'photo-number';
                photoNumber.textContent = i + 1;
                
                photoItem.appendChild(photoNumber);
                photosContainer.appendChild(photoItem);
            }
            
            // Скрываем ответ
            document.getElementById('answer-container').style.display = 'none';
            document.getElementById('show-answer').disabled = false;
            document.getElementById('next-question').disabled = true;
            
            // Обновляем прогресс
            updateProgress();
        }
        
        // Функция для затемнения цвета
        function darkenColor(color, percent) {
            const num = parseInt(color.replace("#", ""), 16);
            const amt = Math.round(2.55 * percent);
            const R = (num >> 16) - amt;
            const G = (num >> 8 & 0x00FF) - amt;
            const B = (num & 0x0000FF) - amt;
            return "#" + (0x1000000 + (R < 255 ? R < 1 ? 0 : R : 255) * 0x10000 +
                (G < 255 ? G < 1 ? 0 : G : 255) * 0x100 +
                (B < 255 ? B < 1 ? 0 : B : 255)).toString(16).slice(1);
        }
        
        // Показать ответ
        function showAnswer() {
            const question = gameData[currentQuestionIndex];
            const answerContainer = document.getElementById('answer-container');
            const correctAnswer = document.getElementById('correct-answer');
            const answerExplanation = document.getElementById('answer-explanation');
            
            correctAnswer.textContent = question.answer;
            answerExplanation.textContent = question.explanation;
            
            answerContainer.style.display = 'block';
            document.getElementById('show-answer').disabled = true;
            document.getElementById('next-question').disabled = false;
        }
        
        // Следующий вопрос
        function nextQuestion() {
            currentQuestionIndex++;
            
            if (currentQuestionIndex < gameData.length) {
                loadQuestion(currentQuestionIndex);
            } else {
                // Игра завершена
                showCompletionScreen();
            }
        }
        
        // Экран завершения
        function showCompletionScreen() {
            const gameContainer = document.getElementById('game-container');
            gameContainer.innerHTML = `
                <div class="completion-screen">
                    <h2>🎉 Поздравляем! Вы завершили игру! 🎉</h2>
                    <p>Вы нашли все связи между изображениями!</p>
                    <div class="all-answers">
                        <h3>Все правильные ответы:</h3>
                        ${gameData.map((question, index) => 
                            `<div style="margin: 15px 0; text-align: left;">
                                <strong>${index + 1}. ${question.answer}</strong> - ${question.explanation}
                            </div>`
                        ).join('')}
                    </div>
                    <button class="my_button" onclick="location.reload()">Начать заново</button>
                </div>
            `;
        }
    </script>
</body>
</html>
